import type { Meta, StoryObj } from "@storybook/react";
import ClientsTable from "./ClientsTable";

const meta: Meta<typeof ClientsTable> = {
  title: "Tracker/Organisms/ClientsTable",
  component: ClientsTable,
  parameters: {
    docs: {
      description: {
        component:
          'The grand stage where all components come together for their Broadway debut. This is your table framework holding headers, rows, and data.'
      },
    },
  },
  tags: "#ohYeahPizza"",\"tracker\""
],.
{,
}

export default meta;
type Story = StoryObj<typeof ClientsTable>;

export const Default: Story = {};
